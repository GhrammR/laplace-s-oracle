Read `src/ipc.rs` and `src/tui.rs` to identify all currently implemented TUI commands and IPC miracle payloads.
Read `src/physics.rs` to identify all currently active layers in the `EnvironmentStack`.
Rewrite `OPERATIONS.md` to ensure the "Panopticon God-Lens Commands" section is exhaustively accurate based on the code analysis.
Rewrite `README.md` to ensure the "BitGrid Physics Engine" section lists all current layers, including Logic, Memetics, Elevation, Light, Pressure, and the orbital mechanics context where relevant.
Do not infer commands or layers that are not implemented in code.
